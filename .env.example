# Shared project notes:
# - Copy backend/.env.example to backend/.env for backend runtime.
# - Copy frontend/.env.local.example to frontend/.env.local for frontend runtime.
# - PostgreSQL is started by docker compose from the repository root.

POSTGRES_DB=problem_record
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

